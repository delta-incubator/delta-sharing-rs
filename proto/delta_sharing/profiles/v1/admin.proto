syntax = "proto3";

package delta_sharing.profiles.v1;

import "delta_sharing/v1/delta_sharing.proto";

message CreateProfileRequest {
  // serialized profile claims
  bytes claims = 1;
}

message CreateProfileResponse {
  // the profile file
  delta_sharing.v1.Profile profile = 1;
}
