{
  "$id": "delta_sharing.catalogs.v1.GetCatalogRequest.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Get a catalog",
  "patternProperties": {
    "^(includeBrowse)$": {
      "description": "Whether to include catalogs in the response for which the principal can only access selective metadata for",
      "type": "boolean"
    }
  },
  "properties": {
    "include_browse": {
      "description": "Whether to include catalogs in the response for which the principal can only access selective metadata for",
      "type": "boolean"
    },
    "name": {
      "description": "Name of catalog.",
      "pattern": "^[a-z][0-9a-z_]*[0-9a-z]$",
      "type": "string"
    }
  },
  "title": "Get Catalog Request",
  "type": "object"
}
